# This class will be to show the steps to each algorithm.
class AlgorithmSteps:
    def __init__(self, window, algorithm):
        ''' m_savedWindow - The window to display info which is needed for the labels.
            m_currentAlgorithm - The current algorithm that is showing its steps. '''
        self.m_savedWindow = window
        self.m_currentAlgorithm = algorithm